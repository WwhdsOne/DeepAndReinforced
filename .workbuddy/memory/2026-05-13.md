# 2026-05-13

## CODEBUDDY.md 生成

- 用户通过 `/init` 请求生成 `CODEBUDDY.md`
- 基于项目已有结构和 `AGENTS.md` 内容生成
- 包含：项目概览、环境命令、目录约定、代码风格、工具优先级、当前实验说明
- 复制 `pyproject.toml` 中的依赖和入口信息

## Claude Code Skills 导入

- 将 `~/.claude/skills/` 下 38 个 skill 导入到 `~/.workbuddy/skills/`
- 跳过了 WorkBuddy 内置的 `skill-creator` 和 `find-skills`
- 实际导入 36 个 skill
- 提示：部分 skill 含 Claude Code 专属机制引用，工作流逻辑可用但工具命令可能不兼容
