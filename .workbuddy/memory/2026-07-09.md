# 2026-07-09 工作日志

- 在 `experiments/ML2021-Spring/HW11/results.md` 新建 HW11 DANN 实验结果记录：
  - 最高分 **0.73274**（无 TTA，200 epoch，单源 DANN）
  - 当前 `hw11_npu.py` 基线最好成绩 **0.73112**（是否含 TTA 待确认）
  - 更正：0.73274(Public) / 0.73112(Private) 为**同一次提交**得到，均无 TTA、200 epoch 单源 DANN。
- 在 `hw11_npu.py` 加回 Entropy Minimization（目标域熵最小化）：`--ent` 默认开、`--no-ent` 关、`--ent-weight` 可调（默认 0.1），权重乘 `adaptive_lambda` 做 ramp-up；`train_epoch` 对目标域预测算熵损失并统计 `ent`、打印。`py_compile` 通过。`results.md` 同步更新。
  - 记录已具备能力（ResNet18 / 标准 DANN / LR 不归零 / TTA / 伪标签 / 均衡推理 / `--mode pseudo`）与待实现增强（EMA Teacher / 迭代二阶段伪标签 / Seed Ensemble）。
- `results.md` 成绩记录表重构（加 Entropy 列），补入用户假设的「当前代码+Entropy+100 epoch」成绩：Public 0.71882 / Private 0.72050，标注"假设未实跑"。
- 用户更正：上述 100 epoch + Entropy 两行实际已在 NPU 跑出（非假设）。已将 `results.md` 标注由"假设未实跑"改为"实际跑出"，备注同步修正。
- **参考 NTU 强基线（Arvin Liu）重写 `hw11_npu.py`**：用户要求"学习他的方法，他没用到的就删除"。保留 ResNet18 backbone（输出512维兼容Decoder）；采纳 loss_balance（类别分布均匀约束*3）、Decoder AE（denoising 重建*3）、source 随机 Canny + ×2 RandomErasing + RandomRotation(30)、target/test 去 Canny、margin 伪标签（top2比<0.5 & top1≥0.3）+ 类反比加权（train_acc≥0.85 启动）、cosine 衰减到 eta_min=1e-6、默认 500 epoch。删除了 TTA、Entropy Minimization、min_lr_ratio(LR不归零)、target Canny。新增 AE 模型与 ae_model.bin 保存、SubsetCustomLabel。py_compile 通过，grep 确认无 TTA/ent/min_lr/target-Canny 残留。`results.md` 能力与备注同步更新（旧分数标注为重写前产出，待重新评估）。
- **半监督训练改进（用户服务器实测 45/500 epoch 即达 0.85，半监督尾巴过长）**：加 `MIN_DANN_EPOCHS=100`（`--min-dann-epochs`，半监督启动前置门槛，防纯伪标签尾巴）+ 半监督阶段用 `ConcatDataset([source_dataset, pseudo_dataset])` 让 source 真实标签与 target 伪标签**联合训练**防漂移/错误累积；伪标签为空时退回 DANN 训练本 epoch。`train_cls_epoch` 现已吃联合数据集。`py_compile` 通过，`results.md` 能力同步更新。用户将重新跑 `python hw11_npu.py`。
- 应要求把进度条从手写函数改为 `tqdm`（用户指定）。`from tqdm import tqdm`；`train_epoch`/`train_cls_epoch` 批次循环 `tqdm(..., leave=True)` 传 `prefix`（`Epoch X/500 [DANN]`/`[semi]`），推理循环 `tqdm(desc="[infer]")`。`py_compile` 通过。注意 NPU 环境需有 tqdm（参考 notebook 也用，通常已装；若 ImportError 则 `pip install tqdm`）。
- **修正进度条显示**：用户反馈不想要外层 500-epoch 总进度条（`训练进度 | 1/500`），要每 epoch 自己的批次条。已删除外层 `tqdm(range(args.epochs), desc="训练进度")`，改为 `for epoch in range(args.epochs):`；内层 batch 条 `leave=False` → `leave=True`，每轮跑完留在屏幕、下一轮另起一行。现输出 = 每 epoch 一个批次进度条 + 该轮 summary 行。
- **编写 HW11 代码演进总结报告**：在 `learning-logs/report/2026-07-09_HW11_DANN代码演进总结.md` 写跨日报告，覆盖 07-08~07-09 全部 7 个阶段（清理→增强v1→Bug修复→Entropy加回→强基线重写→半监督改进→tqdm），含成绩记录、架构图、6 条经验教训、后续规划。
- **精简 results.md**：用户定调——基础款 DANN（ResNet18 + adaptive_lambda + cosine LR + loss_balance + Decoder AE + margin 伪标签）已达 strong baseline（0.73274/0.73112）。未做 TTA / Entropy / EMA / Ensemble，不考虑 boss baseline。去除冗余的能力列表和待评估项。
- **提交推送**：`58b4515`，7 文件变更 +1096/-6269，含重写后 hw11_npu.py、results.md、演进总结日志。
- **HW12 强化学习（LunarLander）完成**：在 `HW12_ZH.ipynb` 中做三处关键改动达到最高要求：
  1. **切换为 DQN 结构**：从原版策略梯度改为 Deep Q-Network，用神经网络近似 Q 值函数
  2. **保存最佳模型**：训练过程中按 reward 保存最优权重为 `best_model.pt`
  3. **增加训练轮次**：提升 episode 数以充分收敛
  - 产出 `best_model.pt` 和 `Action_List_test.npy`
