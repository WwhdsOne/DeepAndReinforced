# 2026-05-21

## 主题：Ch6 PyTorch 版实现 & 代码重构

## 今日完成

### 1. 新增 Ch6 PyTorch 版训练脚本

**文件**: `experiments/fish-book/ch6/torch_train_nn.py`

- 使用 `torch.nn.Module` 重构 Ch6 的双层全连接网络
- 网络结构：`Affine → BatchNorm → ReLU → Dropout → Affine → BatchNorm`
- 权重初始化：
  - 第一层使用 Kaiming 初始化（`nn.init.kaiming_normal_`）
  - 第二层使用 Xavier 初始化（`nn.init.xavier_normal_`）
- 优化器：使用 `torch.optim.Adam`，L2 正则化通过 `weight_decay` 参数实现
- 训练/推理切换：利用 `nn.Module` 自带的 `self.train()` / `self.eval()` 自动控制 Dropout 和 BatchNorm 行为
- 测试结果：
  - 训练准确率：0.9706
  - 测试准确率：0.9657
  - 与 NumPy 版（0.9770 / 0.9702）非常接近

### 2. 重构 common 模块

**文件**: `experiments/fish-book/common/`

- 新增 `__init__.py`，统一导出各层模块
- 新增 `utils.py`，存放公共工具函数（如 `im2col`、`col2im`、`load_mnist`）
- 删除旧的 `network.py`，功能拆分到各层模块
- 更新 `layers.py`，适配新的模块结构

### 3. 提交推送代码

- 提交信息：「新增 ch6 PyTorch 版训练脚本；ch7 卷积网络层实现；重构 common 模块」
- 推送至远程仓库

## 技术要点

### PyTorch 版与 NumPy 版的主要区别

| 组件 | NumPy 版 | PyTorch 版 |
|------|----------|------------|
| 网络定义 | 手动管理 `OrderedDict` | `nn.Module` 子类 |
| 层实现 | `common.layers` 手写 | `nn.Linear`、`nn.BatchNorm1d`、`nn.Dropout` |
| 初始化 | 手写 Kaiming/Xavier | `nn.init.kaiming_normal_`、`nn.init.xavier_normal_` |
| 优化器 | 手写 `Adam` | `torch.optim.Adam` |
| 正则化 | 手动添加 L2 损失 | `weight_decay` 参数 |
| 训练/推理切换 | `train_flg` 参数 | `self.train()` / `self.eval()` |

### PyTorch 训练技巧

- **设备选择**：`device = torch.device("cuda" if torch.cuda.is_available() else "cpu")`
- **学习率调度**：使用 `torch.optim.lr_scheduler.StepLR` 实现每个 epoch 衰减
- **One-hot 转换**：`torch.eye(num_classes)[labels]`
- **准确率计算**：`(preds == labels).float().mean().item()`

## 关键代码路径

```
experiments/fish-book/
├── common/
│   ├── __init__.py                # 新增，统一导出
│   ├── ConvolutionLeayer.py       # 05-19 实现
│   ├── PoolingLayer.py           # 05-19 实现
│   ├── layers.py                  # 更新，适配新结构
│   └── utils.py                   # 新增，工具函数
├── ch6/
│   ├── train_nn.py               # NumPy 版（05-18 完成）
│   └── torch_train_nn.py         # 新增（05-21 PyTorch 版）
└── ch7/
    └── SimpleConvNet.py           # 05-19 实现
```

## 下一步计划

- 完成 Ch7 的卷积网络训练实验（调通 SimpleConvNet 训练流程）
- 对比 NumPy 版和 PyTorch 版卷积网络的性能差异
- 重组 learning-logs 目录为 daily/ 和 report/ 子目录
