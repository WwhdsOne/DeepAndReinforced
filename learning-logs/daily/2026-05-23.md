# 5.23 学习日志

## 今日重点

### KADC2026 要到 100 卡时昇腾 910B3 算力 🎉

在 KADC2026 跟昇腾工程师沟通，成功申请到 **100 卡时** 的昇腾 910B3 云端算力，为后续在国产 AI 芯片上跑实验扫清了资源障碍。服务器环境为 **AlmaLinux**，已安装 `torch_npu==2.9.0` + `torch==2.9.0`，NPU 可正常使用。

## 代码改动（`experiments/fish-book/ch8/train.py`）

### 1. 支持昇腾 NPU 设备检测

- **CUDA / 昇腾 NPU / CPU** 三端自动检测，优先级：CUDA → NPU → CPU
- `auto` 模式下自动识别，无需手动指定设备参数
- 添加 `try: import torch_npu` 静默加载，版本不匹配时自动回退 CPU，不中断训练

### 2. 增加总训练时长统计

- 训练开始时记录 `total_start`
- 训练结束后输出 `总训练时长: Xs (Y.Zmin)`
- 与已有的单 epoch 计时配合
